// styles.ts
import { StyleSheet } from 'react-native';

export const tabNavigatorStyles = {
    activeTintColor: 'tomato', // replace with your color
    inactiveTintColor: 'gray', // replace with your color
};
